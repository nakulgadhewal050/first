export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body >
        <div>
            This is the contact layout
        </div>
        {children}
      </body>
    </html>
  );
}